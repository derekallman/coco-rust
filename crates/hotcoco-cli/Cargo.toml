[package]
name = "hotcoco-cli"
version = "0.1.0"
edition = "2021"
description = "CLI for COCO dataset evaluation"

[dependencies]
hotcoco = { path = "../hotcoco" }
clap = { version = "4", features = ["derive"] }
